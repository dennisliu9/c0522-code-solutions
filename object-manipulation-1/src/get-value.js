/* exported getValue */
/*
Using bracket notation, access the key of the object by specifying key as the key of the object
return that value
*/
function getValue(object, key) {
  return object[key];
}
