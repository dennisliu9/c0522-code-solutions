/* exported getIndexes */
function getIndexes(array) {
  var arrayIndexes = [];
  for (var arrayIdx = 0; arrayIdx < array.length; arrayIdx++) {
    arrayIndexes.push(arrayIdx);
  }
  return arrayIndexes;
}
