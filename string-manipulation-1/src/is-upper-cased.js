/* exported isUpperCased */
/*
If uppercasing the word has no change, it is uppercased
Return the boolean of the condition of the word strictly equal to the word uppercased
*/

function isUpperCased(word) {
  return word === word.toUpperCase();
}
