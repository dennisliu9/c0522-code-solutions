/* exported getFirstChar */
/*
Because strings are array-like, we can access the first character with bracket notation

return the value of the expression where we get the first (0) index of the string
*/
function getFirstChar(string) {
  return string[0];
}
